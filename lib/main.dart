import 'package:flutter/material.dart';
import 'package:session_code_flutter/app/app.dart';

void main() async {
  // final prefs = await SharedPreferences.getInstance();
  // prefs.setBool('onboarding', true);

  runApp(const MyApp());
}
